const os = require("os");

// Get platform
console.log(os.platform());

// Get arch
console.log(os.arch());

// Get cpu info
console.log(os.cpus());

// Get free memory
console.log(os.freemem());

// Get total memory
console.log(os.totalmem());
